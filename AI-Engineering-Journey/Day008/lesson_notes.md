Lists: This is a collection of multiples values stored in single variable

Indexing: Indexing is the process of accessing elements in a sequence using their position.

List Methods: This are built in functions used to modify a list

Loops with Lists: This allows you iterate through a list

Tuples: A tuple is similar to a list but the only difference is that it is immutable


            Interview Solution
Why does Python start list indexes at 0 instead of 1?
 Ans: Python uses a zero based indexing format that is why it starts from zero

Explain the difference between a list and a tuple as if you were teaching a friend.
 Ans: A list is a collection of multiple values stored in a single variable. A tuple is similar to a list but a list is mutable while a tuple is immutable


If you wanted to store the names of every student in a university, why would a list be better than creating thousands of variables?
Ans:A list lets us store all the student names in one variable. This keeps the code organized, makes it easier to add or remove students, and allows us to process every student using loops instead of creating thousands of separate variables. 
