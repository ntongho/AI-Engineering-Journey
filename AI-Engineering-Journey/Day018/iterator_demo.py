# Write:

# numbers = [10, 20, 30, 40, 50]

# Then create an iterator using:

# iter()

# Use:

# next()

# to manually print the numbers one at a time.

numbers = [10, 20, 30, 40, 50] 
number_iterator = iter(numbers)

print(next(number_iterator))